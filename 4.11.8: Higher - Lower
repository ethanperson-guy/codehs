# Set your secret number to 3.3312 to grade your code
# Your code here...
float_num = 3.3312
guess = float(input("Enter a guess: "))
while True:
    if guess == float_num:
        print "Correct!"
        break
    elif guess < float_num:
        print "Too low!"
        guess = float(input("Enter a guess: "))
    else:
        print "Too high!"
        guess = float(input("Enter a guess: "))
